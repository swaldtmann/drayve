# Hetzner Cloud API token for Molecule tests (creates real servers).
# Copy this file to .env.test and fill in your token:
#   cp .env.test.example .env.test
#
# Get a token: Hetzner Cloud Console → Project → Security → API Tokens
# The token needs read/write permissions.
HCLOUD_TOKEN=your-hcloud-token-here
